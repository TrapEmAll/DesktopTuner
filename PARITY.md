# StartAllBack parity tracker

StartAllBack's public product description is the baseline: a classic-style Start menu, a redesigned taskbar, File Explorer and Control Panel styling, context-menu changes, and cross-app visual consistency. This tracker separates current companion-app behavior from remaining product gaps. “Implemented” describes Desktop Tuner features; it does not imply that Windows' built-in shell has been replaced or that a feature has passed interactive desktop validation.

| Area | Capability | Implemented in Desktop Tuner | Missing or unverified |
|---|---|---|---|
| Start | Customizable system-place menu | Settings hide and reorder Documents, This PC, Control Panel, Network, Recent items, Recycle Bin, and Run. Documents, Downloads, Music, Pictures, and Videos have nested folder flyouts. The Control Panel flyout opens Control Panel and offers 27 configurable classic applets/canonical items, including Bluetooth Devices, Add Hardware, Game Controllers, Tablet PC Settings, Network and Sharing Center, Internet Options, Credential Manager, Security and Maintenance, and Ease of Access Center. Applet shortcuts can be hidden/reordered; direct `.cpl` entries are hidden when their modules are absent from the Windows system directory. In Shell Launcher mode, filesystem-backed places and their folder flyouts open in Desktop Tuner Explorer; This PC and Recycle Bin use their specialized views, while Network and Control Panel use the companion Shell namespace browser. | Windows can still redirect an installed applet to Settings or omit it on an edition. Canonical `/name` items are not availability-probed; the namespace browser still lacks full native verb handling. |
| Start | Search and launch apps | Companion launcher indexes Start-menu shortcuts and `shell:AppsFolder` on a background STA thread; ranks exact, prefix, word-boundary, substring, and bounded typo-tolerant matches; shows alphabet sections and shell icons; can open shortcut locations or elevate desktop shortcuts/executables through UAC; in shell-host mode, shortcut locations open in companion Explorer with the target selected; hands unmatched Enter searches to Windows Search; and offers explicit Windows and Web search. `Ctrl+Alt+Space` and optional Windows-key replacement with modified-key passthrough are supported. | The built-in Windows Start menu is not replaced or restyled by the companion launcher. Packaged Windows apps are not offered elevation. |
| Start | Menu styles, pins, and navigation | Modern, Classic, Compact, Windows 7-inspired two-column, Windows 8-inspired tile-grid, and Windows 10-inspired app-list/tile layouts are available. The Windows 7-inspired layout now browses categorized Start-menu folders when search is empty and switches to ranked results while searching. Windows 8/10 layouts support renameable tile groups and four tile sizes. Pins can be created from app results, folder trees, `.lnk`/`.exe`/folder drops, and context menus; app pins reorder by drag or menu; filesystem-backed Start entries can also be pinned directly to the custom taskbar from Start context menus. The launcher also supports recent-app counts and clearing history, nested Start folders, bounded system-place browsing, Settings/This PC/Control Panel/Network/Recent/Recycle Bin/Run links, power actions, and the local account name/photo fallback. Start placement can center along the selected taskbar edge. | Authentic Windows 7/8 Start styling and native Windows Start integration remain absent; packaged Start entries without filesystem launch targets cannot be pinned to the custom taskbar. |
| Start | Recycle Bin shortcut | Start can show a configurable Recycle Bin link. Companion Explorer lists deleted items with original paths and deletion dates, restores selections, empties the bin with confirmation, and confirms permanent deletion. | Windows Shell still supplies the underlying Recycle Bin namespace and operations. |
| Taskbar | Replacement bar and edge placement | Custom bars overlay the primary display or all connected displays, with per-display placement, a Search button, window switching, optional startup, and opt-in replacement mode that captures/hides native taskbars and restores their visibility after normal or unexpected exit. Taskbar settings can now start and exit the all-edition shell overlay directly, and shell-host settings plus the taskbar menu expose an explicit “Exit shell replacement and start Explorer” recovery action. Shell-host sessions keep at least one replacement taskbar available by disabling the close-bar commands. `--shell-overlay` composes custom desktop, Start, and bars while Explorer remains running. Edge-docked bars use the AppBar API for work area, activation, full-screen yielding, and cascade/tile handling; shell-host mode requests this work-area reservation independently for each display without an integrated Explorer notification area and keeps the bar as an overlay if Windows declines it; normal replacement mode exits and restores Windows' taskbar if reservation fails. | Windows 11 interactive validation is outstanding. Deep Explorer integration and the native Start/taskbar relationship are not reproduced. Floating bars are overlay-only. |
| Taskbar | Labels, sizing, grouping, and drag/drop | Windows 11/10/7-inspired visual presets; optional labels; three icon sizes; compact/standard/relaxed spacing; centered or left app buttons; launch pins, Win+1–9 activation, Win+Ctrl+1–9 activation of the most recently active window for a pin, Win+Shift+1–9 new-instance launch, Ctrl+Win+Shift+1–9 elevated launch for supported pins, and middle-click or context-menu new-instance launch for pinned apps; running app groups expose guarded Start new instance, End task, and Open file location commands when their executable path is available; shell-host Win+T/Win+Shift+T next/previous taskbar focus with arrow-key navigation and primary-first cycling across connected displays; grouping/ungrouping and additional instances; folder flyouts; elevated shortcut launch; reorderable running-window buttons; shared DWM previews; minimize/close gestures; executable, shortcut, folder, and document drag/drop; and three bar sizes and a persisted Lock the taskbar command are supported. Running pinned app buttons expose the same guarded End task action when a live process is available. In shell-host mode, pinned folders open in companion Explorer, while pinned executable and shortcut locations open the parent folder with the target selected. | Native taskbar integration remains absent; the display order and focus behavior still need live comparison with Explorer on Windows 11, including modifier shortcuts. |
| Taskbar | Segments, translucency, and auto-hide | Overlay transparency is adjustable from 0–70%, with adaptive opacity when maximized apps cover a display. DWM Desktop Acrylic is used where available, with themed fallback; taskbar accents follow Windows; optional Aura/Dynamic Aura highlights, idle auto-hide and Win+Alt+T toggle, hide-on-maximize, floating inset, separate Start/apps/system segments, and an apps-only centered dock are available. | Native shell integration remains absent. Acrylic and auto-hide collision behavior require interactive Windows validation. |
| Taskbar | Tray, menus, widgets, weather, and flyouts | Custom rounded, palette-aware menus and DWM previews; minimize/close group commands; direct Task Manager and Show the desktop commands from the taskbar background menu; the clock opens Notification Center and exposes Date and time and Notification settings; Widgets, Task View, emoji, and Show Desktop buttons; in `--shell-host` mode, right-clicking Start and pressing Win+X open the same custom Power User menu with Settings, installed apps, common administration tools, Desktop Tuner Explorer, Start search, Run, desktop toggle, an explicit confirmed “Exit shell replacement and start Explorer” recovery action, and the existing lock/sleep/hibernate/sign-out/shutdown/restart actions, while Win+B focuses the custom system area on the foreground window's display, prioritizing the custom tray control and keeping its controls navigable by arrows with Escape returning to the prior app; Win+A, Win+N, Win+S, Win+Space, Win+W, and Win+Tab open Quick Settings, Notification Center, Windows Search, the keyboard-layout picker, Widgets, and Task View through their existing Windows surfaces; the custom Network and Battery buttons open Quick Settings on click and retain direct Network and Power settings links in their context menus; Win+R opens a companion Run dialog that parses quoted command lines, expands environment variables, opens filesystem folders in companion Explorer, optionally requests UAC elevation, and keeps a clearable, 12-entry MRU command history with prefix autocomplete; Win+M minimizes eligible top-level windows and Win+Shift+M restores the windows captured by that shortcut; Win+D and the Show Desktop button remain a separate toggle that keeps Desktop Tuner desktop/taskbars visible, then restore windows minimized by the previous toggle; opt-in city/postal-code weather with localized temperature units, current WMO conditions, and a Widgets-board action; audio output and microphone controls; configurable system buttons; regional clock/seconds with a settings toggle that writes the Windows Explorer-compatible preference; Quick Settings, Network, keyboard layout, On-Screen Keyboard, and battery controls; dynamic button-space calculation; and edge-aware exposure of the native tray are available. In `--shell-host` mode, an Explorer notification area is integrated when available, and live discovery switches between its tray and custom system controls while registering or releasing the custom AppBar reservation; Jump List filesystem folders and Shell namespace locations also route into companion Explorer. Weather is off by default, uses an explicit selected location, and attributes Open-Meteo and GeoNames. | A shell-host session with no Explorer taskbar still cannot host arbitrary third-party notification icons, and exact native Windows flyout/overflow behavior remains incomplete. Run, Win+B, Win+X, Win+R, Win+M, Win+Shift+M, Win+A, Win+N, Win+S, Win+Space, Win+W, and Win+Tab have automated shortcut coverage but still need live shell-host validation against Windows behavior; the companion dialog does not duplicate the native Run dialog's full visual treatment. Audio device selection uses an undocumented policy COM interface with Settings fallback. The public weather endpoint is licensed for non-commercial use; commercial deployments require a licensed endpoint. Live tray/flyout validation remains. |
| Taskbar | Multi-display and virtual desktop buttons | Running windows can appear on every custom bar, only their Windows-assigned display, or that display plus the primary bar. Spanning windows use largest-monitor intersection; pinned launch buttons appear everywhere. The documented virtual desktop manager filters to the active desktop by default, can include all desktops, and fails open if desktop state is unavailable. | Live physical multi-monitor and virtual-desktop behavior needs Windows desktop validation. |
| Explorer | Window, tabs, layouts, and file operations | Companion Explorer uses Mica where available; supports multi-tab folder browsing, Details/List/Small/Medium/Large/Tiles views, per-folder sort/view preferences, shell icons, tab reorder/transfer/duplicate/close/restore, middle-click folder tabs, Ctrl+N, selection-aware actions, single-selection Pin to Start and Pin to taskbar for folders and executable/shortcut launch targets, multi-select cut/copy/paste/delete, drag-to-folder transfers with Explorer-like move/copy defaults, native single/multi-item Properties, and native Shell menus through Show more options. | Native File Explorer's ribbon/command bar and visual styling are not changed. |
| Explorer | Details pane and search | A resizable, hideable bottom details pane shows type, path, size, dates, and common locations, including aggregate size and shared/differing dates for selections. Recursive current-folder search supports names, bounded plain-text contents, metadata, quoted phrases, wildcards, extension/kind filters, modified-date bounds, and binary size comparisons. | Native File Explorer pane placement and search visuals remain unchanged. |
| Explorer | Folder defaults and navigation | Home/This PC startup; recent files honoring Windows' recent-items privacy setting; breadcrumbs with lazy child menus; Ctrl+L/Alt+D, Alt+Up/Alt+Left/Alt+Right, and Ctrl+Shift+N; extension/hidden/compact/full-path/separate-process preferences; persistent quick-access folders; expandable asynchronous navigation tree; restored tabs, selections, views, sorting, six Details column widths, history, pane state, and folder-tab preference; and optional grouped drives with free-space bars are supported. | The native Windows command bar/ribbon styling is not changed. |
| Explorer | Dark mode and common dialogs | Companion Start, Explorer, Settings, cards, and title bars follow Windows' app palette; companion windows request Mica with a solid fallback and use the DWM dark frame where supported. | Built-in common-dialog contents and Control Panel do not receive Desktop Tuner dark styling. |
| Explorer | Context-menu appearance and behavior | Companion Start/taskbar/Explorer menus use rounded, live light/dark colors and DWM Desktop Acrylic with solid fallbacks. Touch digitizers use 44-DIP targets; pointer menus use compact spacing. A per-user classic full-menu switch is available. | Native Windows context-menu appearance is not restyled. |
| Explorer | Open locations from Windows Shell | Optional per-user folder/background commands and Win+E routing open locations in companion Explorer; existing regular and shell-host app instances receive paths. The “Open with Desktop Tuner” command forwards into the running replacement shell instead of launching a second, inert process. A native `IExplorerCommand` bridge and sparse identity manifest cover the modern Windows 11 menu; CI compiles and validates them. | Distribution of the modern extension awaits a trusted package-signing certificate. Legacy entries may appear under Show more options. Native Explorer remains the default folder handler and can still open locations. |
| Explorer | File rename | F2, the Rename command, and context actions edit names inline in all companion Explorer views. The Shell namespace browser also supports F2 inline rename for items advertising `SFGAO_CANRENAME`, using `IShellFolder.SetNameOf`. File extensions are excluded from the initial selection; dotfiles/folders are fully selected. Enter commits, Escape cancels, and focus loss commits. | Companion filesystem Explorer still uses its file-operation rename path; provider-specific Shell rename behavior is only used in the namespace browser and replacement desktop. |
| Shell-wide | Applet behavior, fonts, colors, consistency | Companion Start and Explorer follow the Windows app light/dark mode and accent; taskbar and previews follow system mode/accent live. | Native Win32 app recoloring, restored classic applet behavior, and shell-wide font styling remain absent. |
| Shell-wide | Desktop, wallpaper, icons, and actions | Prototype `--desktop-host` spans connected displays, including negative-origin monitors; mirrors Windows per-monitor wallpaper images, background color, and Center/Tile/Stretch/Fit/Fill/Span placement; refreshes after desktop preference changes and polls for slideshow advances; persists per-monitor icon positions; migrates disconnected-monitor icons to the primary display; enumerates root virtual entries from the Windows desktop Shell alongside user/public Desktop files; watches desktop folders and registers recursive Shell change notifications so virtual items and icons refresh; supports click/Ctrl/Shift selection, Ctrl+A, Escape, arrow-key icon navigation, Enter-to-open, Alt+Enter native Properties for selected items, Delete and Shift+Delete through native Shell verbs for selected filesystem and virtual items, Shift+F10 item context menus, F2 inline rename for filesystem items and virtual Shell items advertising `SFGAO_CANRENAME` through `IShellFolder.SetNameOf`, drag-to-select marquees with Ctrl-toggle, group drag including virtual Shell icons, Explorer-aware drag-in, Ctrl+C/Ctrl+X/Ctrl+V native Shell clipboard copy/cut/paste with cut-state dimming, and native Shell `IDataObject` drag-out for filesystem and virtual selections, including mixed selections rooted in the desktop Shell namespace, plus combined native context menus for selected items in the same Shell folder, native filesystem/namespace/background menus, refresh, folder creation, and single-selection Pin to Start and Pin to taskbar commands for filesystem folders and executable/shortcut launch targets. “More desktop options” opens the actual Windows desktop Shell background menu, exposing its registered New and personalization verbs. Desktop context options sort by name/type/size/date modified, align icons to a grid, and auto-arrange per display with saved preferences. In Shell Launcher replacement mode, opening filesystem folders, This PC, and Recycle Bin from the desktop routes into Desktop Tuner Explorer; other discovered namespace folders open in its namespace browser, which supports nested navigation, Back/Forward/Up, address entry, six Explorer layouts, recursive search with Explorer name/metadata/content filters, Shell icons, Ctrl+A and Escape selection control, F2 inline rename and a Rename context command for items advertising `SFGAO_CANRENAME`, native Properties from the item menu and Alt+Enter, Delete and Shift+Delete through native Shell verbs, native Shell clipboard copy/cut/paste with Ctrl+C/Ctrl+X/Ctrl+V and item-menu commands (including mixed-parent selections), keyboard invocation of native context menus, native Shell `IDataObject` drag-out for selected items across Shell parents, multi-item Open via the registered Shell handlers and companion windows, extended selection, combined native context menus for selected items in the same Shell folder, native background menus, and automatic refresh after recursive Shell notifications with Refresh/F5/Ctrl+R fallback. `Win+E` opens companion Explorer automatically in shell-host mode. The supervisor waits for the replacement taskbars to be created, work-area setup to be attempted, and the first taskbar frame to render, times out after 60 seconds, retries once, and starts Explorer if startup fails repeatedly. After readiness, a UI-thread heartbeat every 10 seconds lets it recover a shell that stops responding for 45 seconds. Namespace search can be canceled and stops at a 128-level safety limit; content search applies only to supported filesystem text files. | Shell replacement is not equivalent to Explorer's complete desktop shell: shell extensions, all native item verbs, and exact interaction behavior remain incomplete. Shell clipboard cut-state dimming follows clipboard changes and detects external filesystem and virtual Shell cuts when the source publishes the native Preferred DropEffect plus CF_HDROP or Shell IDList Array clipboard formats. Copy/cut/paste still needs live validation across filesystem and virtual namespace providers. Virtual items without `SFGAO_CANRENAME` remain non-renamable; rename success still depends on the namespace provider accepting `SetNameOf`. Animated and HDR wallpapers are not supported by the image-backed desktop surface. Actual sign-in, hung-start recovery, wallpaper/slideshow updates, and managed-edition behavior still need live validation. |
| Reliability | Compatibility, recovery, and resources | User-level settings snapshots and rollback; opt-in startup; per-user installer; debounced display reconciliation; shared 200 ms top-level window snapshots; captured taskbar-visibility restoration; watchdog handling of unexpected exits; PID start-time verification; abandoned-snapshot recovery; and shell-host readiness monitoring with a 60-second startup timeout plus a UI-thread heartbeat that detects 45-second hangs, retries once, then starts Explorer are implemented. CI builds/tests Windows Server 2022 and 2025 images, with package artifacts produced on 2025. | Live Windows 10/11 compatibility and shell sign-in/recovery validation, updater, and telemetry-free resource profiling remain outstanding. |

## Validation and shell-integration notes

When selected Shell items come from different parent folders, Delete queues the full selection in one native `IFileOperation`; normal Delete requests undo/recycle behavior and Shift+Delete requests permanent deletion. Same-parent selections continue to use the provider's native Shell verb. For multiple filesystem-backed items from different folders, Properties opens one merged Shell property sheet; selections containing virtual items retain provider-native per-item Properties because `SHMultiFileProperties` requires filesystem parsing paths. These paths let heterogeneous desktop selections share native Shell operations where Windows supports them.

Custom taskbar app context menus expose the app's native Recent and Frequent Jump List destinations when its AppUserModelID is available from the pinned Shell item or a running window. Windows' documented [`IApplicationDocumentLists`](https://learn.microsoft.com/en-us/windows/win32/api/shobjidl_core/nn-shobjidl_core-iapplicationdocumentlists) API only enumerates Recent and Frequent destinations through [`GetList`](https://learn.microsoft.com/en-us/windows/win32/api/shobjidl_core/nf-shobjidl_core-iapplicationdocumentlists-getlist); pinned destinations, custom categories, and app task commands remain missing from the custom taskbar.

The companion Explorer maps mouse side buttons to Back and Forward in the active tab's folder history. Replacement-mode auto-hide registers edge-docked bars with the per-monitor AppBar API and releases the registration when disabled or closed; if an edge is already claimed, Desktop Tuner retains local auto-hide behavior. These code paths are covered by automated checks, but live reveal/collision behavior still needs Windows 11 desktop validation.

Windows [Shell Launcher](https://learn.microsoft.com/en-us/windows/configuration/shell-launcher/) can replace `Explorer.exe` as the logon shell on Enterprise, Education, and IoT Enterprise editions; it also removes Explorer's desktop shell. Desktop Tuner can configure and restore an isolated current-user Shell Launcher mapping when the optional Windows feature is already enabled and no existing mappings are present; it preserves the previous default and enablement state and uses Explorer for other accounts. Windows 11 Pro additionally supports Microsoft's per-user [Custom User Interface policy](https://learn.microsoft.com/en-us/windows/client-management/mdm/policy-csp-admx-winlogon#customshell), which starts an alternate interface instead of Explorer. Desktop Tuner detects its own configured executable and enters `--shell-host` mode; settings can set/restore that per-user policy on supported editions. The shell entry point runs a separate worker under a bounded supervisor, retries one failed start, and starts Explorer after a normal exit or exhausted retry. After repeated failure, it clears the per-user policy only if it still targets Desktop Tuner, preventing the same broken shell from being selected at next sign-in. These opt-in paths remain unvalidated at sign-in and recovery on live managed Windows desktops.

The all-edition `--shell-overlay` mode hosts the companion desktop, Start, and taskbars while Explorer remains in the background. It covers Explorer's main taskbar controls and exposes the native notification area on supported edge-aligned layouts. It is an overlay, not a Winlogon shell replacement; it does not replace Explorer windows or shell services and does not prevent Explorer from starting. Keep the Windows taskbar available until live recovery testing is complete.

The current Windows App SDK and Win32 public documentation do not expose APIs for replacing native taskbar internals or arbitrary built-in Start visuals. Those gaps need compatibility-tested shell integration and a Windows-build test matrix; registry toggles alone are insufficient. See [SHELL-INTEGRATION.md](SHELL-INTEGRATION.md) for setup, known limits, and recovery instructions.
