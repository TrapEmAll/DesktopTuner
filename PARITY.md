# StartAllBack parity tracker

StartAllBack's public product description is the baseline: a classic-style Start menu, a redesigned taskbar, File Explorer and Control Panel styling, context-menu changes, and cross-app visual consistency. This tracker separates current companion-app behavior from remaining product gaps. “Implemented” describes Desktop Tuner features; it does not imply that Windows' built-in shell has been replaced or that a feature has passed interactive desktop validation.

| Area | Capability | Implemented in Desktop Tuner | Missing or unverified |
|---|---|---|---|
| Start | Customizable system-place menu | Settings hide and reorder Documents, This PC, Control Panel, Network, Recent items, Recycle Bin, and Run. Documents, Downloads, Music, Pictures, and Videos have nested folder flyouts. The Control Panel flyout opens Control Panel and offers 27 configurable classic applets/canonical items, including Bluetooth Devices, Add Hardware, Game Controllers, Tablet PC Settings, Network and Sharing Center, Internet Options, Credential Manager, Security and Maintenance, and Ease of Access Center. Applet shortcuts can be hidden/reordered; direct `.cpl` entries are hidden when their modules are absent from the Windows system directory. In Shell Launcher mode, filesystem-backed places and their folder flyouts open in Desktop Tuner Explorer; This PC and Recycle Bin use their specialized views, while Network and Control Panel use the companion Shell namespace browser. | Windows can still redirect an installed applet to Settings or omit it on an edition. Canonical `/name` items are not availability-probed; the namespace browser still lacks full native verb handling. |
| Start | Search and launch apps | Companion launcher indexes Start-menu shortcuts and `shell:AppsFolder` on a background STA thread; ranks exact, prefix, word-boundary, substring, and bounded typo-tolerant matches; shows alphabet sections and shell icons; can open shortcut locations or elevate desktop shortcuts/executables through UAC; hands unmatched Enter searches to Windows Search; and offers explicit Windows and Web search. `Ctrl+Alt+Space` and optional Windows-key replacement with modified-key passthrough are supported. | The built-in Windows Start menu is not replaced or restyled by the companion launcher. Packaged Windows apps are not offered elevation. |
| Start | Menu styles, pins, and navigation | Modern, Classic, Compact, Windows 7-inspired two-column, Windows 8-inspired tile-grid, and Windows 10-inspired app-list/tile layouts are available. Windows 8/10 layouts support renameable tile groups and four tile sizes. Pins can be created from app results, folder trees, `.lnk`/`.exe`/folder drops, and context menus; app pins reorder by drag or menu. The launcher also supports recent-app counts and clearing history, nested Start folders, bounded system-place browsing, Settings/This PC/Control Panel/Network/Recent/Recycle Bin/Run links, power actions, and the local account name/photo fallback. Start placement can center along the selected taskbar edge. | Authentic Windows 7/8 Start styling and native Windows Start integration remain absent. |
| Start | Recycle Bin shortcut | Start can show a configurable Recycle Bin link. Companion Explorer lists deleted items with original paths and deletion dates, restores selections, empties the bin with confirmation, and confirms permanent deletion. | Windows Shell still supplies the underlying Recycle Bin namespace and operations. |
| Taskbar | Replacement bar and edge placement | Custom bars overlay the primary display or all connected displays, with per-display placement, a Search button, window switching, optional startup, and opt-in replacement mode that captures/hides native taskbars and restores their visibility after normal or unexpected exit. Taskbar settings can now start and exit the all-edition shell overlay directly. `--shell-overlay` composes custom desktop, Start, and bars while Explorer remains running. Edge-docked bars use the AppBar API for work area, activation, full-screen yielding, and cascade/tile handling; replacement mode exits and restores Windows' taskbar if reservation fails. | Windows 11 interactive validation is outstanding. Deep Explorer integration and the native Start/taskbar relationship are not reproduced. Floating bars are overlay-only. |
| Taskbar | Labels, sizing, grouping, and drag/drop | Windows 11/10/7-inspired visual presets; optional labels; three icon sizes; compact/standard/relaxed spacing; centered or left app buttons; launch pins and Win+1–9/Win+T navigation; grouping/ungrouping and additional instances; folder flyouts; elevated shortcut launch; reorderable running-window buttons; shared DWM previews; minimize/close gestures; executable, shortcut, folder, and document drag/drop; and three bar sizes are supported. | Native taskbar integration remains absent; behavior has not been validated across a live Windows build matrix. |
| Taskbar | Segments, translucency, and auto-hide | Overlay transparency is adjustable from 0–70%, with adaptive opacity when maximized apps cover a display. DWM Desktop Acrylic is used where available, with themed fallback; taskbar accents follow Windows; optional Aura/Dynamic Aura highlights, idle auto-hide and Win+Alt+T toggle, hide-on-maximize, floating inset, separate Start/apps/system segments, and an apps-only centered dock are available. | Native shell integration remains absent. Acrylic and auto-hide collision behavior require interactive Windows validation. |
| Taskbar | Tray, menus, widgets, weather, and flyouts | Custom rounded, palette-aware menus and DWM previews; minimize/close group commands; Widgets, Task View, emoji, and Show Desktop buttons; opt-in city/postal-code weather with localized temperature units, current WMO conditions, and a Widgets-board action; audio output and microphone controls; configurable system buttons; regional clock/seconds; Quick Settings, Network, keyboard layout, On-Screen Keyboard, and battery controls; dynamic button-space calculation; and edge-aware exposure of the native tray are available. Weather is off by default, uses an explicit selected location, and attributes Open-Meteo and GeoNames. | Native Windows flyouts and notification-area hosting are incomplete, especially in `--shell-host` mode. Audio device selection uses an undocumented policy COM interface with Settings fallback. The public weather endpoint is licensed for non-commercial use; commercial deployments require a licensed endpoint. Live tray/flyout validation remains. |
| Taskbar | Multi-display and virtual desktop buttons | Running windows can appear on every custom bar, only their Windows-assigned display, or that display plus the primary bar. Spanning windows use largest-monitor intersection; pinned launch buttons appear everywhere. The documented virtual desktop manager filters to the active desktop by default, can include all desktops, and fails open if desktop state is unavailable. | Live physical multi-monitor and virtual-desktop behavior needs Windows desktop validation. |
| Explorer | Window, tabs, layouts, and file operations | Companion Explorer uses Mica where available; supports multi-tab folder browsing, Details/List/Small/Medium/Large/Tiles views, per-folder sort/view preferences, shell icons, tab reorder/transfer/duplicate/close/restore, middle-click folder tabs, Ctrl+N, selection-aware actions, multi-select cut/copy/paste/delete, drag-to-folder transfers with Explorer-like move/copy defaults, native single/multi-item Properties, and native Shell menus through Show more options. | Native File Explorer's ribbon/command bar and visual styling are not changed. |
| Explorer | Details pane and search | A resizable, hideable bottom details pane shows type, path, size, dates, and common locations, including aggregate size and shared/differing dates for selections. Recursive current-folder search supports names, bounded plain-text contents, metadata, quoted phrases, wildcards, extension/kind filters, modified-date bounds, and binary size comparisons. | Native File Explorer pane placement and search visuals remain unchanged. |
| Explorer | Folder defaults and navigation | Home/This PC startup; recent files honoring Windows' recent-items privacy setting; breadcrumbs with lazy child menus; Ctrl+L/Alt+D, Alt+Up/Alt+Left/Alt+Right, and Ctrl+Shift+N; extension/hidden/compact/full-path/separate-process preferences; persistent quick-access folders; expandable asynchronous navigation tree; restored tabs, selections, views, sorting, six Details column widths, history, pane state, and folder-tab preference; and optional grouped drives with free-space bars are supported. | The native Windows command bar/ribbon styling is not changed. |
| Explorer | Dark mode and common dialogs | Companion Start, Explorer, Settings, cards, and title bars follow Windows' app palette; companion windows request Mica with a solid fallback and use the DWM dark frame where supported. | Built-in common-dialog contents and Control Panel do not receive Desktop Tuner dark styling. |
| Explorer | Context-menu appearance and behavior | Companion Start/taskbar/Explorer menus use rounded, live light/dark colors and DWM Desktop Acrylic with solid fallbacks. Touch digitizers use 44-DIP targets; pointer menus use compact spacing. A per-user classic full-menu switch is available. | Native Windows context-menu appearance is not restyled. |
| Explorer | Open locations from Windows Shell | Optional per-user folder/background commands and Win+E routing open locations in companion Explorer; existing app instances receive paths. A native `IExplorerCommand` bridge and sparse identity manifest cover the modern Windows 11 menu; CI compiles and validates them. | Distribution of the modern extension awaits a trusted package-signing certificate. Legacy entries may appear under Show more options. Native Explorer remains the default folder handler and can still open locations. |
| Explorer | File rename | F2, the Rename command, and context actions edit names inline in all views. File extensions are excluded from the initial selection; dotfiles/folders are fully selected. Enter commits, Escape cancels, and focus loss commits. | Windows Shell rename integration remains absent. |
| Shell-wide | Applet behavior, fonts, colors, consistency | Companion Start and Explorer follow the Windows app light/dark mode and accent; taskbar and previews follow system mode/accent live. | Native Win32 app recoloring, restored classic applet behavior, and shell-wide font styling remain absent. |
| Shell-wide | Desktop, wallpaper, icons, and actions | Prototype `--desktop-host` spans connected displays, including negative-origin monitors; persists per-monitor icon positions; migrates disconnected-monitor icons to the primary display; enumerates root virtual entries from the Windows desktop Shell alongside user/public Desktop files; watches desktop folders and registers recursive Shell change notifications so virtual items and icons refresh; supports selection, group drag including virtual Shell icons, Explorer-aware drag-in, filesystem drag-out, and combined native context menus for selected items in the same Shell folder, plus native filesystem/namespace/background menus, refresh, and folder creation. Desktop context options sort by name/type/size/date modified, align icons to a grid, and auto-arrange per display with saved preferences. In Shell Launcher replacement mode, opening filesystem folders, This PC, and Recycle Bin from the desktop routes into Desktop Tuner Explorer; other discovered namespace folders open in its namespace browser, which supports nested navigation, Back/Forward/Up, address entry, six Explorer layouts, recursive search with Explorer name/metadata/content filters, Shell icons, extended selection, combined native context menus for selected items in the same Shell folder, native background menus, and automatic refresh after recursive Shell notifications with Refresh/F5/Ctrl+R fallback. `Win+E` opens companion Explorer automatically in shell-host mode. The supervisor waits for the replacement taskbars to be created, work-area setup to be attempted, and the first taskbar frame to render, times out after 60 seconds, retries once, and starts Explorer if startup fails repeatedly. Namespace search can be canceled and stops at a 128-level safety limit; content search applies only to supported filesystem text files. | Shell replacement is not equivalent to Explorer's complete desktop shell: shell extensions, all native item verbs, and exact interaction behavior remain incomplete. Actual sign-in, hung-start recovery, and managed-edition behavior still need live validation. |
| Reliability | Compatibility, recovery, and resources | User-level settings snapshots and rollback; opt-in startup; per-user installer; debounced display reconciliation; shared 200 ms top-level window snapshots; captured taskbar-visibility restoration; watchdog handling of unexpected exits; PID start-time verification; abandoned-snapshot recovery; and shell-host startup readiness monitoring with a 60-second timeout, one retry, and Explorer fallback are implemented. CI builds/tests Windows Server 2022 and 2025 images, with package artifacts produced on 2025. | Live Windows 10/11 compatibility and shell sign-in/recovery validation, updater, and telemetry-free resource profiling remain outstanding. |

## Validation and shell-integration notes

The companion Explorer maps mouse side buttons to Back and Forward in the active tab's folder history. Replacement-mode auto-hide registers edge-docked bars with the per-monitor AppBar API and releases the registration when disabled or closed; if an edge is already claimed, Desktop Tuner retains local auto-hide behavior. These code paths are covered by automated checks, but live reveal/collision behavior still needs Windows 11 desktop validation.

Windows [Shell Launcher](https://learn.microsoft.com/en-us/windows/configuration/shell-launcher/) can replace `Explorer.exe` as the logon shell on Enterprise, Education, and IoT Enterprise editions; it also removes Explorer's desktop shell. Desktop Tuner can configure and restore an isolated current-user Shell Launcher mapping when the optional Windows feature is already enabled and no existing mappings are present; it preserves the previous default and enablement state and uses Explorer for other accounts. Windows 11 Pro additionally supports Microsoft's per-user [Custom User Interface policy](https://learn.microsoft.com/en-us/windows/client-management/mdm/policy-csp-admx-winlogon#customshell), which starts an alternate interface instead of Explorer. Desktop Tuner detects its own configured executable and enters `--shell-host` mode; settings can set/restore that per-user policy on supported editions. The shell entry point runs a separate worker under a bounded supervisor, retries one failed start, and starts Explorer after a normal exit or exhausted retry. After repeated failure, it clears the per-user policy only if it still targets Desktop Tuner, preventing the same broken shell from being selected at next sign-in. These opt-in paths remain unvalidated at sign-in and recovery on live managed Windows desktops.

The all-edition `--shell-overlay` mode hosts the companion desktop, Start, and taskbars while Explorer remains in the background. It covers Explorer's main taskbar controls and exposes the native notification area on supported edge-aligned layouts. It is an overlay, not a Winlogon shell replacement; it does not replace Explorer windows or shell services and does not prevent Explorer from starting. Keep the Windows taskbar available until live recovery testing is complete.

The current Windows App SDK and Win32 public documentation do not expose APIs for replacing native taskbar internals or arbitrary built-in Start visuals. Those gaps need compatibility-tested shell integration and a Windows-build test matrix; registry toggles alone are insufficient. See [SHELL-INTEGRATION.md](SHELL-INTEGRATION.md) for setup, known limits, and recovery instructions.
